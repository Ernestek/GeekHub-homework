"""7. Write a script to concatenate
all elements in a list into a string
and print it. List must include both
strings and integers and must be hardcoded."""

my_list = ['Hello', 2022, 'Me', 22]
print(' '.join(map(str, my_list)))
